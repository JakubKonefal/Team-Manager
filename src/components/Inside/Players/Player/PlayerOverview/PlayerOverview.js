import React from "react";
import classes from "./PlayerOverview.module.css";

const PlayerOverview = props => {
  return <div className={classes.PlayerOverview}></div>;
};

export default PlayerOverview;
